fastapi>=0.124.0
uvicorn>=0.29.0
requests>=2.31.0
pydantic>=2.12.0
python-dotenv>=1.0.0
groq>=0.4.0

# Optional ML dependencies - install separately if needed
# These require compilation and may fail on free hosting tiers
# The app works without them using fallback methods
# 
# To install locally:
# pip install sentence-transformers torch spacy
# python -m spacy download en_core_web_sm
